#include<stdio.h>
int main()
{
  int fact=1,n;
  printf("Enter a value:");
  scanf("%d",&n);
  if(n<0){
      printf("Factorial can't be generated");
  }
  else
  {
    for(int i=1;i<=n;i++)
    {
      fact*=i;
    }
    printf("Factorial : %d",fact);
  }
  return 0;
}